//1.a WAP TO insert a new element at the specified position in the given linear array.
//1.b WAP to delete any specified element from the given linear array.
//2.a WAP to find the sum of upper traiangular of any given square matrix.
//2.b WAP to find the sum of lower traiangular of any given SQUARE matrix
//2.c WAP TO find the sum of principle diagonal elements of the given square matrix.
//2.d WAP to print the Z shaped from the given square matrix.
// 2.e  WAP to print the X shaped from the given square matrix.
#include <stdio.h>
void main()
{
    int A[10],i,n;
    printf("Enter No. of Elements of Matrix A: ");
    scanf("%d", &n);
    printf("Enter Elements of Matrix A: ");
    for (int i = 0; i < n;i++)
    {
        scanf("%d", &A[i][j);
    }
    printf("position to be inse");
    for (int i = ; i < n;i++)
    {
        scanf("%d", &A[i][j]);
    }
    
}