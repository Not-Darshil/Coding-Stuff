#include<stdio.h>
typedef struct node 
{
    int data;
    struct node *next;
}node;

void main()
{ node *head=NULL;
int n,i;
printf("Enter No. of Nodes: ");
scanf("%d",&n);
p=(node *)malloc(sizeof(node));
printf("Enter Value: ");
scanf("%d",&head->next);
Q=head
for (i=0;i<n-1;i++)
{
    p=(node *)malloc(sizeof(node));
    printf("Enter Value: ");
    scanf("%d",&p->data);
    P->next=NULL;
    P->
}
    
}